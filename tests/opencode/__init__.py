"""Tests for the OpenCode adapter.

The usage-emitter suite here is JavaScript (`test-opsx-usage-emitter.js`) and
runs under node, not unittest. This package marker exists so that any Python
test added alongside it is picked up by `unittest discover` rather than
silently skipped.
"""
