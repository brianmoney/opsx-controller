# Stack
- Python 3.11+ standard library; orchestrator uses `tomllib`; no project virtualenv or dependency install required for normal tests.
- Node runtime is used for the OpenCode usage-emitter test.
- OpenSpec CLI >= 1.7 is an external runtime dependency for change operations.
- Supported coding clients: OpenCode, Claude Code, Codex CLI.